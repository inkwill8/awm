// we have a given array and must sort it in decreasing order

// so first the array must be sorted correctly and then reversed

let array = [5, 2, 1, -10, 8];

let sorted = array.sort();

let arrayDecreasing = sorted.reverse();

console.log(arrayDecreasing);
console.log(array);
